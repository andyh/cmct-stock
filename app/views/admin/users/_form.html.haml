= simple_form_for [:admin, @user] do |f|
  = f.input :first_name
  = f.input :last_name
  = f.input :email
  = f.input :password
  = f.input :role, collection: User::VALID_ROLES.map {|role| [ role.humanize, role ]}
  = f.input :active

  .actions
    = f.submit
