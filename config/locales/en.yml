en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: CMCT
  helpers:
    submit:
      user:
        create: "Create User"
  user:
    create:
      success: User was successfully created.
    update:
      success: User was successfully updated.
    delete:
      success: "%{user} was successfully removed."
    remove_self_attempt:
      failure: You cannot remove your own user account

  unauthorised_access: You are not authorized to perform this action
  admin:
    users:
      page_title: User Management
